spring.application.name=OrderService

# Port
server.port=8382

# -----------------------
# Database
# -----------------------
spring.datasource.url=jdbc:mysql://localhost:3306/ordersorder_service?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=root
# # -----------------------
# # Naming entity mapping table in SQL
# # -----------------------
# spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
# # -----------------------
# # Show sql
# # -----------------------
# spring.jpa.properties.hibernate.format_sql=true
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# # Cấu hình Eureka Client
# eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

# # Đăng ký với Eureka Server
# eureka.client.register-with-eureka=true

# # Lấy danh sách các service từ Eureka Server
# eureka.client.fetch-registry=true

# Cấu hình kết nối RabbitMQ Server
spring.rabbitmq.host=127.0.0.1
spring.rabbitmq.port=5672
spring.rabbitmq.username=tanvt
spring.rabbitmq.password=123@
spring.rabbitmq.virtual-host=/


